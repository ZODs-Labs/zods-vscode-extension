{
	{{#each codepoints}}
	"zods-{{@key}}": {
		"description": "{{@key}} icon",
		"default": {
			"fontPath": "out/zodsicons.woff2",
			"fontCharacter": "\\\{{codepoint this}}"
		}
	{{#if @last}}
	}
	{{else}}
	},
	{{/if}}
	{{/each}}
}
